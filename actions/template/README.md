# Template Action

This is the starter source folder for a LUMA Action.

Validate it:

```bash
npm run action:validate -- actions/template
```

Compile it:

```bash
npm run action:pack -- actions/template
```

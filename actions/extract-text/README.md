# Extract Text

LUMA Action that lets the user drag over a region of the screen, runs OCR on that selected fragment, and copies the detected text to the clipboard.

This Action carries its OCR runtime in `action/vendor/`.

```bash
npm run action:validate -- actions/extract-text
npm run action:pack -- actions/extract-text
```

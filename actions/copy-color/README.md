# Copiar Color

First LUMA Action.

It opens a compact panel, activates the system eyedropper when available, and copies the selected HEX value to the clipboard.

Build:

```bash
npm run action:pack -- actions/copy-color
```
